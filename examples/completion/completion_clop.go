// Code generated by clop-gen; DO NOT EDIT.

package main

import (
	"encoding/json"
	"fmt"
	"github.com/BurntSushi/toml"
	"gopkg.in/yaml.v3"
	"os"
	"path/filepath"
	"strings"
)

// Options的Parse方法，用于解析命令行参数
func (c *Options) Parse(args []string) error {
	if len(args) == 0 {
		args = os.Args[1:]
	}

	// 检查是否显示版本信息
	for i := 0; i < len(args); i++ {
		arg := args[i]
		if arg == "-v" ||
			arg == "--version" {
			fmt.Printf("Options\n")
			return nil
		}
	}

	// 检查是否有子命令
	if len(args) > 0 && !strings.HasPrefix(args[0], "-") {
		subcommand := args[0]
		subcommandArgs := args[1:]

		switch subcommand {

		case "Add":
			return c.Add.Parse(subcommandArgs)

		case "Remove":
			return c.Remove.Parse(subcommandArgs)

		default:
			return fmt.Errorf("未知子命令: %s", subcommand)
		}
	}

	// 检查是否指定了配置文件
	var configFile string
	for i := 0; i < len(args); i++ {
		if args[i] == "--config" || args[i] == "-c" {
			if i+1 < len(args) {
				configFile = args[i+1]
				// 移除 --config 和它的值，避免后续解析错误
				if i+2 < len(args) {
					args = append(args[:i], args[i+2:]...)
				} else {
					args = args[:i]
				}
				i--
				break
			}
		}
	}

	// 从配置文件加载选项
	if configFile != "" {
		if err := c.loadFromConfigFile(configFile); err != nil {
			return fmt.Errorf("加载配置文件失败: %w", err)
		}
	}

	for i := 0; i < len(args); i++ {
		arg := args[i]
		switch {
		case arg == "--help" || arg == "-h":
			c.Usage()
			os.Exit(0)

		case arg == "-h" || arg == "--help":

			c.Help = true

		case arg == "-v" || arg == "--version":

			fmt.Printf("Options\n")
			return nil

		case arg == "-d" || arg == "--debug":

			c.Debug = true

		case arg == "-i" || arg == "--input":

			if i+1 >= len(args) {
				return fmt.Errorf("missing value for -i or --input")
			}

			c.Input = args[i+1]

			i++

		case arg == "-o" || arg == "--output":

			if i+1 >= len(args) {
				return fmt.Errorf("missing value for -o or --output")
			}

			c.Output = args[i+1]

			i++

		case arg == "--dir":

			if i+1 >= len(args) {
				return fmt.Errorf("missing value for --dir")
			}

			c.Dir = args[i+1]

			i++

		case arg == "-f" || arg == "--format":

			if i+1 >= len(args) {
				return fmt.Errorf("missing value for -f or --format")
			}

			c.Format = args[i+1]

			i++

		default:
			return fmt.Errorf("unknown option: %s", arg)
		}
	}

	return nil
}

// UsageTmpl 是用于生成 Usage 函数的模板
func (c *Options) Usage() {
	fmt.Println(`USAGE:
  myapp [SUBCOMMAND] [OPTIONS]

SUBCOMMANDS:
    Add    添加记录
    Remove    删除记录

OPTIONS:
    -c, --config    指定配置文件路径 (支持 JSON, YAML, TOML)
    -h, --help      显示帮助信息
    -h, --help    显示帮助信息
    -v, --version    显示版本信息
    -d, --debug    启用调试模式
    -i, --input    输入文件路径
    -o, --output    输出文件路径
    --dir    工作目录
    -f, --format    输出格式
`)
}

func (c *Options) loadFromConfigFile(configFile string) error {
	data, err := os.ReadFile(configFile)
	if err != nil {
		return fmt.Errorf("读取配置文件失败: %w", err)
	}

	// 根据文件扩展名选择解析方式
	ext := filepath.Ext(configFile)
	switch strings.ToLower(ext) {
	case ".json":
		return json.Unmarshal(data, c)
	case ".yaml", ".yml":
		return yaml.Unmarshal(data, c)
	case ".toml":
		_, err := toml.Decode(string(data), c)
		return err
	default:
		return fmt.Errorf("不支持的配置文件格式: %s", ext)
	}
}
